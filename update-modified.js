const { listAllMdFiles } = require('./lib/file-management');
const { readFileModifiedMatter } = require('./lib/front-matter');


const main = () => {
  // script running code
};

if (require.main === module) { main(); }